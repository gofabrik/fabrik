change_type: breaking

component: web

note: Move sectioned template loading into web, replace web.View with chainable template responses, and add fragments, regions, and server-sent event streams.

issues: [0]
