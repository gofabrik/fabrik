Templates load into an injectable *fabrikweb.Templates; declared helpers land
in the generated FuncMap, package-qualified wherever they live.
-- go.mod --
module app

go 1.27

require github.com/gofabrik/fabrik/web v0.0.0

replace github.com/gofabrik/fabrik/web => WEBDIR
-- main.go --
package main

import "os"

func main() { os.Exit(run()) }
-- web/templates.go --
package web

import (
	"embed"
	"strings"
	"time"
)

//fabrik:web:templates
//go:embed all:templates
var Templates embed.FS

//fabrik:web:templates:func
func HumanizeAge(t time.Time) string { return t.String() }

//fabrik:web:templates:func name=upper
func UpperCase(s string) string { return strings.ToUpper(s) }
-- shared/helpers.go --
package shared

//fabrik:web:templates:func
func Reverse(s string) string {
	b := []byte(s)
	for i, j := 0, len(b)-1; i < j; i, j = i+1, j-1 {
		b[i], b[j] = b[j], b[i]
	}
	return string(b)
}
-- web/templates/_default/_layout.html --
<html><body>{{ block "content" . }}{{ end }}</body></html>
-- web/templates/_default/home.html --
{{ define "content" }}<h1>{{ upper .Title }}</h1>{{ end }}
-- web/web.go --
package web

import (
	"net/http"

	fabrikweb "github.com/gofabrik/fabrik/web"
)

type Handlers struct {
	Templates *fabrikweb.Templates
}

//fabrik:http GET /
func (h *Handlers) Home(w http.ResponseWriter, r *http.Request) {
	h.Templates.Render(w, "home", "", map[string]string{"Title": "hi"})
}
-- want/main.gen.go --
// Code generated by fabrik. DO NOT EDIT.
// Regenerate with: fabrik wire

package main

import (
	"fmt"
	"os"

	"github.com/gofabrik/fabrik/router"
	web2 "github.com/gofabrik/fabrik/web"

	"app/shared"
	"app/web"
)

func run() int {
	if err := func() (err error) {
		// Providers
		r := router.New()
		webTemplates, err := web2.LoadTemplates(web.Templates, "templates", web2.FuncMap{
			"humanizeAge": web.HumanizeAge,
			"reverse":     shared.Reverse,
			"upper":       web.UpperCase,
		})
		if err != nil {
			return err
		}
		webHandlers := &web.Handlers{
			Templates: webTemplates,
		}

		// Register
		r.Method("GET", "/", webHandlers.Home)
		return nil
	}(); err != nil {
		fmt.Fprintln(os.Stderr, "app:", err)
		return 1
	}
	return 0
}
