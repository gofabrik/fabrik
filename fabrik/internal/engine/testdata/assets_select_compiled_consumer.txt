Switch mode cannot bind *assets.Compiled because source mode only
satisfies assets.Server.
-- go.mod --
module app

go 1.27

require (
	github.com/gofabrik/fabrik/assets v0.0.0
	github.com/gofabrik/fabrik/config v0.0.0
)

replace (
	github.com/gofabrik/fabrik/assets => ASSETSDIR
	github.com/gofabrik/fabrik/config => CONFIGDIR
)
-- main.go --
package main

import "os"

func main() { os.Exit(run()) }
-- web/assets.go --
package web

import (
	"embed"

	"github.com/gofabrik/fabrik/assets"
)

//fabrik:assets
//go:embed all:assets
var Assets embed.FS

//fabrik:config assets
type AssetsConfig = assets.Options
-- web/assets/app.css --
body {}
-- web/web.go --
package web

import (
	"net/http"

	"github.com/gofabrik/fabrik/assets"
)

type Handlers struct {
	Assets *assets.Compiled
}

//fabrik:http GET /{$}
func (h *Handlers) Home(w http.ResponseWriter, r *http.Request) {}
-- want/diags --
error: web/web.go:10:2: no provider for *assets.Compiled (field Assets of web.Handlers)
  help: with a source/compiled asset switch only assets.Server is injectable; take assets.Server
