A //fabrik:cron handler binds an injectable *jobs.Runner with scheduling enabled but does not start it.
-- go.mod --
module app

go 1.26

require (
	github.com/gofabrik/fabrik/cli v0.0.0
	github.com/gofabrik/fabrik/jobs v0.0.0
)

replace github.com/gofabrik/fabrik/cli => CLIDIR
replace github.com/gofabrik/fabrik/jobs => JOBSDIR
-- main.go --
package main

import "os"

func main() { os.Exit(run()) }
-- shared/shared.go --
package shared

import (
	"context"

	"github.com/gofabrik/fabrik/cli"
	"github.com/gofabrik/fabrik/jobs"
)

//fabrik:provider
func NewJobStore() (jobs.Store, error) { return jobs.NewMemoryStore(), nil }

//fabrik:cron name=purge schedule="0 * * * *"
func Purge(ctx context.Context) error { return nil }

//fabrik:cli:command
func Work(ctx cli.Context, worker *jobs.Runner) error {
	return worker.Run(ctx)
}
-- want/main.gen.go --
// Code generated by fabrik. DO NOT EDIT.
// Regenerate with: fabrik wire

package main

import (
	"context"
	"os"
	"os/signal"
	"syscall"

	"github.com/gofabrik/fabrik/cli"
	"github.com/gofabrik/fabrik/jobs"

	"app/shared"
)

func run() int {
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	sigc := make(chan os.Signal, 2)
	signal.Notify(sigc, os.Interrupt, syscall.SIGTERM)
	defer signal.Stop(sigc)
	go func() {
		<-sigc
		cancel()
		<-sigc
		os.Exit(1)
	}()

	root := &cli.Command{
		Name: "app",
		Subcommands: []*cli.Command{
			{
				Name: "work",
				Run: func(ctx cli.Context) error {
					runner, err := buildWork(ctx)
					if err != nil {
						return err
					}
					return shared.Work(ctx, runner)
				},
			},
		},
	}
	return root.Exec(os.Args[1:], cli.WithSignalContext(ctx))
}

func buildWork(ctx context.Context) (*jobs.Runner, error) {
	// Providers
	sharedJobsStore, err := shared.NewJobStore()
	if err != nil {
		return nil, err
	}
	jobsManager, err := jobs.New(sharedJobsStore, jobs.Config{})
	if err != nil {
		return nil, err
	}

	// Register
	// Jobs
	if err := jobs.RegisterCron(jobsManager, "purge", "0 * * * *", func(c jobs.Context) error {
		return shared.Purge(c)
	}); err != nil {
		return nil, err
	}
	return jobs.NewRunner(jobsManager, jobs.RuntimeConfig{RunScheduler: true}), nil
}
