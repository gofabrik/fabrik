Directives in package main warn and are ignored; generation continues.
-- go.mod --
module app

go 1.26
-- main.go --
package main

import "net/http"

//fabrik:http GET /main
func InMain(w http.ResponseWriter, r *http.Request) {}

//fabrik:http:group /main
type InMainGroup struct{}

//fabrik:http:static /main
var inMainVar int

func main() {
	if err := run(); err != nil {
		panic(err)
	}
}
-- web/web.go --
package web

import "net/http"

//fabrik:http GET /
func Index(w http.ResponseWriter, r *http.Request) {}
-- want/main.gen.go --
// Code generated by fabrik. DO NOT EDIT.

package main

import (
	"app/web"
	"net/http"
	"os"

	"github.com/gofabrik/fabrik/router"
)

func run() error {
	// Providers
	r := router.New()

	// Routes
	r.Method("GET", "/", web.Index)

	// Serve
	addr := ":8080"
	if p := os.Getenv("PORT"); p != "" {
		addr = ":" + p
	}
	return http.ListenAndServe(addr, r)
}
-- want/diags --
warning: main.go:5:1: //fabrik:http in package main is ignored
  help: move it to a subpackage
warning: main.go:8:1: //fabrik:http:group in package main is ignored
  help: move it to a subpackage
warning: main.go:11:1: //fabrik:http:static in package main is ignored
  help: move it to a subpackage
