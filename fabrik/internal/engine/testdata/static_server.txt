A //fabrik:http:static tree binds an injectable *httpserver.Server without other routes.
-- go.mod --
module app

go 1.26

require (
	github.com/gofabrik/fabrik/cli v0.0.0
	github.com/gofabrik/fabrik/httpserver v0.0.0
)

replace github.com/gofabrik/fabrik/cli => CLIDIR
replace github.com/gofabrik/fabrik/httpserver => HTTPSERVERDIR
-- main.go --
package main

import "os"

func main() { os.Exit(run()) }
-- shared/shared.go --
package shared

import (
	"embed"

	"github.com/gofabrik/fabrik/cli"
	"github.com/gofabrik/fabrik/httpserver"
)

//fabrik:http:static /static dir=static
//go:embed static
var Static embed.FS

//fabrik:cli:command
func Serve(ctx cli.Context, server *httpserver.Server) error {
	return server.Run(ctx)
}
-- shared/static/robots.txt --
User-agent: *
-- want/main.gen.go --
// Code generated by fabrik. DO NOT EDIT.
// Regenerate with: fabrik wire

package main

import (
	"context"
	"io/fs"
	"net/http"
	"os"
	"os/signal"
	"syscall"

	"github.com/gofabrik/fabrik/cli"
	"github.com/gofabrik/fabrik/httpserver"
	"github.com/gofabrik/fabrik/router"

	"app/shared"
)

func run() int {
	ctx, cancel := context.WithCancel(context.Background())
	defer cancel()
	sigc := make(chan os.Signal, 2)
	signal.Notify(sigc, os.Interrupt, syscall.SIGTERM)
	defer signal.Stop(sigc)
	go func() {
		<-sigc
		cancel()
		<-sigc
		os.Exit(1)
	}()

	root := &cli.Command{
		Name: "app",
		Subcommands: []*cli.Command{
			{
				Name: "serve",
				Run: func(ctx cli.Context) error {
					server, err := buildServe(ctx)
					if err != nil {
						return err
					}
					return shared.Serve(ctx, server)
				},
			},
		},
	}
	return root.Exec(os.Args[1:], cli.WithSignalContext(ctx))
}

func buildServe(ctx context.Context) (*httpserver.Server, error) {
	// Providers
	// embedded Static, served under /static
	sharedStatic, err := fs.Sub(shared.Static, "static")
	if err != nil {
		return nil, err
	}

	r := router.New()

	// Register
	r.Handle("/static/", http.StripPrefix("/static", http.FileServerFS(sharedStatic)))
	return httpserver.New(r, nil), nil
}
